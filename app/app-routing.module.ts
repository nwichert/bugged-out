import { NgModule } from '@angular/core';
import { RouterModule } from '@angular/router';


@NgModule({
  imports: [ 
    RouterModule.forRoot([
      // Define URL paths within here:
      
    ])
  ],
  exports: [ RouterModule ]
})
export class AppRoutingModule { }